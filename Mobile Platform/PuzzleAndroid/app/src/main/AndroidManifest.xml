<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.puzzleandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/cp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Mode"></activity>
        <activity android:name=".Rank" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".GameActivity9" />
        <activity android:name=".GameActivity24" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>